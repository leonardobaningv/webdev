<p id="notice"><%= notice %></p>

<h1>Listing Order Lists</h1>

<table>
  <thead>
    <tr>
      <th>Food</th>
      <th>Event</th>
      <th>Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_lists.each do |order_list| %>
      <tr>
        <td><%= order_list.food_id %></td>
        <td><%= order_list.event_id %></td>
        <td><%= order_list.quantity %></td>
        <td><%= link_to 'Show', order_list %></td>
        <td><%= link_to 'Edit', edit_order_list_path(order_list) %></td>
        <td><%= link_to 'Destroy', order_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order list', new_order_list_path %>
